[workspace]
resolver = "2"
members = ["crates/compositor"]

# Shared metadata inherited by member crates via `field.workspace = true`.
[workspace.package]
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "A fast, minimal Wayland desktop for Linux."
